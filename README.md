
<!-- Иконки технологий с градиентом -->
<p align="center">
  <img src="https://skillicons.dev/icons?i=python,django,docker,ubuntu,git,github,linux,bash&theme=dark&perline=8" />
</p>

# DjangoBlog
web + python3 + django

# Мой путь: Запуск Django блога в Docker на WSL 🐧🐳

Всем привет! Меня зовут `@Gabryelf`, и я создал этот проект не только как простой Django блог, но и как полигон для тестирования современных методов разработки и деплоя.

Главная задача была не написать код (с ним всё просто), а пройти полноценный путь "от винды до продакшена": поднять Linux-окружение на Windows, приручить Docker и запустить всё это хозяйство в контейнерах.

Результатом этого эксперимента и является данный репозиторий. Здесь вы найдете мой минималистичный Django-проект, а в этом `README` — подробнейший гайд о том, как я это сделал.

---

## 🎯 Цель эксперимента

1.  Установить подсистему Linux (WSL) на чистый Windows.
2.  Развернуть в `bash` на Linux Docker и Docker Compose.
3.  Клонировать свой проект прямо в консоль Linux.
4.  Запустить Django-приложение в связке контейнеров.
5.  Убедиться, что всё работает и доступно из браузера Windows.

Спойлер: у меня получилось! И сейчас я расскажу, как повторить этот трюк.

---

## 📝 Пошаговое руководство

### Шаг 1: Установка WSL (Windows Subsystem for Linux)

Первым делом нужно дать Windows возможность запускать Linux. Открываем **PowerShell** или **Командную строку** от имени администратора и вводим магическую команду:

```powershell
wsl --install
```

Эта команда автоматически включит все необходимые компоненты Windows, скачает и установит Ubuntu (дистрибутив по умолчанию).
После установки компьютер попросит перезагрузку. **Перезагружаемся!**

Но лично я скачал Ubuntu из Microsoft Store. По некоторым оценкам это не так хардово, зато все так же быстро.
Для этого нужно прежде открыть `Пуск` на Windows и найти `Включение и выключение компонентов Windows`, там поставить
галочки на пунктах `Подсистема Windows для Linux` и бутнуть компьютер.

После перезагрузки откроется окно установки Ubuntu. Нужно будет придумать имя пользователя и пароль. Запоминаем их, они понадобятся для `sudo`.

**Готово!** У нас есть полноценный Linux внутри Windows.

### Шаг 2: Знакомство с консолью Linux и подготовка

Запускаем Ubuntu из меню «Пуск». Теперь мы в мире Linux-команд. Обновим список пакетов и установим свежие версии всего, что есть:

```bash
sudo apt update && sudo apt upgrade -y
```

*Пароль вводим тот, что задавали при установке Ubuntu.*

### Шаг 3: Установка Docker в Ubuntu

Docker — это сердце нашего проекта. Установим его официальным способом.

1.  Скачиваем и запускаем официальный скрипт для установки Docker:
    ```bash
    curl -fsSL https://get.docker.com -o get-docker.sh
    sudo sh get-docker.sh
    ```
2.  Добавляем своего пользователя в группу `docker`, чтобы не вводить `sudo` перед каждой командой `docker`.
    ```bash
    sudo usermod -aG docker $USER
    ```
3.  **ВАЖНО:** Чтобы изменения вступили в силу, нужно **закрыть терминал Ubuntu и открыть его заново**, или
     ввести команду `newgrp docker`, это даст возможность не перезапускать терминал.
   
4.  Проверяем, что Docker установлен и работает:
    ```bash
    docker --version
    docker ps
    ```
    Если команда `docker ps` выполняется без ошибок и не требует `sudo` — мы молодцы.

### Шаг 4: Установка Docker Compose

Docker Compose поможет нам управлять многоконтейнерной установкой (Django + База данных). Установим плагин и 
проверим версию.
> Так же Docker Compose может быть уже установлен в системе вместе с докером, в этом случае просто проверим версию 
заранее.

Проверяем версию:
```bash
docker compose version
```

```bash
sudo apt-get install docker-compose-plugin
```

### Шаг 5: Клонируем репозиторий с проектом
Если вы еще не настроили Git в терминале Ubuntu, значит набираем следующие команды, или
просто пропускаем этот шаг.

```bash
git config --global user.name "Твое имя аккаунта на git hub именно в ковычках"
git config --global user.email "Название почты которая привязана к аккаунту тоже в ковычках"
```

Теперь, когда всё готово, клонируй мой проект прямо в домашнюю директорию Linux, или можешь попробовать
свой в качестве тестов, но этот рабочий!

```bash
git clone https://github.com/Gabryelf/DjangoBlog.git
cd DjangoBlog
```

Команда `ls` покажет нам содержимое: `DjangoBlog/` — наш проект.

### Шаг 6: Подготовка переменных окружения

Для безопасности и гибкости мы используем файл `.env`. В репозитории есть пример — `.env_example`. Скопируем его и при необходимости отредактируем.

```bash
cp .env_example .env
```

Если хочешь, можешь открыть файл и изменить пароли от БД или секретный ключ Django:
```bash
nano .env
# Или любой другой редактор (vim, code)
```
> Если ты размещаешь впервые и используешь мой репо, можешь не настраивать переменные в этот раз.

Так же нужно убедиться что файлы `Dockerfile`, `docker-compose.yml` и `requirements.txt` есть в проекте и 
заполнену примерно так:

> requirements.txt
````python
Django==5.2.10
Pillow
python-dotenv
````

> Dockerfile
````python
FROM python:3.11

ENV PYTHONUNBUFFERED=1
ENV PYTHONDONTWRITEBYTECODE=1

WORKDIR /DjangoBlog

COPY requirements.txt /DjangoBlog/

RUN pip install --no-cache-dir -r requirements.txt

COPY . /DjangoBlog/

CMD ["python", "manage.py", "runserver", "0.0.0.0:8000"]
````

> docker-compose.yml
````python
version: '3.11'

services:
  web:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - .:/DjangoBlog
    environment:
      - DEBUG=1
````
> Если файлов нет - создаем с помощью `nano` и имя файла
> сохранить `Ctrl + O`, `Enter` и `Ctrl + X` выход!

### Шаг 7: Запуск проекта в Docker Compose

Вот он, кульминационный момент! Запускаем сборку контейнеров в фоновом режиме.

```bash
docker compose up -d --build
```

> Так же я размещу несколько нужных команд для `docker compose` здесь, что бы можно было избежать сложных ситуаций
> ```bash
> `docker compose down`  --- остановка контейнеров
> `docker compose down -v`  --- остановка + удаление томов БД
> `docker compose up --build`  --- пересборка после изменений
> `docker compose up -d`  --- запуск в фоне
> `docker compose stop`  --- остановка фоновых контейнеров
> ```

Docker начнет скачивать образ Python, PostgreSQL (данный репо без нее), копировать туда наш код и настраивать сеть. Это займет пару минут.

### Шаг 8: Применение миграций и создание суперпользователя

Проект запущен, но база данных пока пуста. Зайдем в консоль работающего контейнера с Django и накатим миграции.

```bash
cd DjangoBlog
docker compose exec web python manage.py migrate
```

Внутри контейнера выполняем стандартные команды Django:
```bash
docker compose exec web python manage.py createsuperuser  # Создаем админа для входа в /admin
exit  # Выходим из контейнера если находимся в нем, а не в директории проекта!
```

### Шаг 9: Проверка результата

Открываем браузер на нашей Windows-машине и переходим по адресу: `http://localhost:8000`

🎉 **Вуаля!** Мы видим наш Django-блог. Linux, Docker и код — всё работает вместе как часы.

---

## 🚀 Команды для управления проектом

*   **Остановить контейнеры:**
    ```bash
    docker compose down
    ```
*   **Пересобрать и запустить:**
    ```bash
    docker compose up -d --build
    ```
*   **Посмотреть логи:**
    ```bash
    docker compose logs -f
    ```
*   **Зайти в контейнер с Django:**
    ```bash
    docker exec -it djangoblog-web-1 bash
    ```

---

## 💡 Выводы

Этот эксперимент наглядно показал, насколько бесшовной стала разработка на Windows с использованием WSL и Docker. Мы получили изолированное, легко воспроизводимое окружение для нашего Django-проекта, которое ничем не отличается от "настоящего" Linux-сервера. К тому же интегированная
Ubuntu может работать не только с Docker и Python, но и с Node.js

Наслаждайтесь блогом и спасибо, что дочитали до конца! 😉

**Happy coding!** 🐍✨

